version: "3"
services:
  front:
    build: ./kitchenowl
    restart: unless-stopped
    # environment:
    #   - BACK_URL=back:5000 # Change this if you rename the containers
    depends_on:
      - back
  back:
    build: ./backend
    restart: unless-stopped
    environment:
      - JWT_SECRET_KEY=PLEASE_CHANGE_ME
      - FRONT_URL=https://kitchenowl.nexus-agency.net
    volumes:
      - kitchenowl_data:/data

volumes:
  kitchenowl_data:
